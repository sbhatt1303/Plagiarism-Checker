To run the program put all the files of program in same folder.
Edit the files "int1.txt" and "int2.txt" and paste the required documents to check for the similartity between them.
Then open the terminal and navigate to the project directory and run the command "bash Run.sh".
The output will be available in file "out1.txt".
